const data = {
    name: "Folajomi Shotunde",
    email: "folajor.mee@gmail.com",
    id: "HNG-06103",
    language: "JavaScript"
}

const output = () => `Hello world, this is ${data.name} with HNG ID ${data.id}, and email ${data.email} using ${data.language} for stage 2 task`

console.log(output());